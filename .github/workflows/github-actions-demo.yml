name: GitHub Actions Demo
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
    - name: Restore dependencies
      run: dotnet restore ./src/Faff.sln
    - name: Build
      run: dotnet build ./src/Faff.sln --no-restore
    - name: Test
      run: dotnet test ./src/Faff.Tests/Faff.Tests.csproj      
    - run: echo "🍏 This job's status is ${{ job.status }}."
